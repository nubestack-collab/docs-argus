# Build dependencies for the ARGUS documentation site.
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt
#   mkdocs serve
mkdocs==1.6.1
mkdocs-material==9.7.6
pymdown-extensions==11.0
